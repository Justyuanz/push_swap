#include "push_swap.h"

void	ft_ra(t_vec *stack_a)
{
	int tmp;
	int	i;

	i = 1;
	if (!stack_a || !stack_a->memory)
		return;
	tmp = stack_a->memory[0];
	while(i < stack_a->len)
	{
		stack_a->memory
		i++;
	}
}